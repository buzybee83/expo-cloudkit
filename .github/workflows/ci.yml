name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: Typecheck / Lint / Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --ci --coverage

      - name: Build
        run: npm run build

  swift-tests:
    name: Swift XCTest (iOS Simulator)
    runs-on: macos-14
    continue-on-error: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install example dependencies
        working-directory: example
        run: npm install --legacy-peer-deps

      - name: Prebuild Expo project (generate ios/)
        working-directory: example
        run: npx expo prebuild --platform ios --no-install

      # Inject the module's Swift test files into the generated Xcode test target.
      # The xcodeproj gem (bundled with Xcode's Ruby) adds each .swift file in
      # ios/Tests/ to the ExpoCloudKitExampleTests group and target so that
      # xcodebuild compiles them with @testable import ExpoCloudKit access.
      - name: Inject Swift test sources into Xcode test target
        run: |
          gem install xcodeproj --no-document --quiet
          ruby - <<'RUBY'
          require 'xcodeproj'

          PROJECT_PATH = 'example/ios/ExpoCloudKitExample.xcodeproj'
          TEST_TARGET_NAME = 'ExpoCloudKitExampleTests'
          TESTS_DIR = File.expand_path('ios/Tests')

          project = Xcodeproj::Project.open(PROJECT_PATH)

          test_target = project.targets.find { |t| t.name == TEST_TARGET_NAME }
          raise "Test target '#{TEST_TARGET_NAME}' not found in project" if test_target.nil?

          # Find or create a group for the injected test sources.
          group = project.main_group.find_subpath(TEST_TARGET_NAME, true)
          group.set_path(TESTS_DIR)

          swift_files = Dir.glob(File.join(TESTS_DIR, '*.swift'))
          raise 'No Swift files found in ios/Tests/' if swift_files.empty?

          swift_files.each do |file_path|
            file_ref = group.new_file(file_path)
            test_target.source_build_phase.add_file_reference(file_ref)
            puts "Added: #{File.basename(file_path)}"
          end

          project.save
          puts "Saved #{PROJECT_PATH}"
          RUBY

      - name: Cache CocoaPods downloads
        uses: actions/cache@v4
        with:
          path: ~/.cocoapods
          key: cocoapods-${{ runner.os }}-${{ hashFiles('example/ios/Podfile') }}
          restore-keys: |
            cocoapods-${{ runner.os }}-

      - name: Install CocoaPods
        working-directory: example/ios
        run: pod install --repo-update

      - name: Install xcpretty
        run: gem install xcpretty --no-document --quiet

      - name: Run Swift tests
        working-directory: example/ios
        run: |
          set -o pipefail
          xcodebuild test \
            -workspace ExpoCloudKitExample.xcworkspace \
            -scheme ExpoCloudKitExample \
            -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=latest" \
            -only-testing:ExpoCloudKitExampleTests \
            2>&1 | xcpretty --color
